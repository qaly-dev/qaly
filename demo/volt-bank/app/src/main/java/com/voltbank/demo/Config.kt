package com.voltbank.demo

object Config {
    // DEMO VERSION SWITCH
    // 1 = "Send Money"     (record the qaly test with this)
    // 2 = "Transfer Funds" (change to this to show the auto-heal)
    const val DEMO_VERSION = 1
}
